apiVersion: v1
kind: Service
metadata:
  name: {{ include "recipeapp.fullname" . }}
  labels:
    {{- include "recipeapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "recipeapp.selectorLabels" . | nindent 4 }}
